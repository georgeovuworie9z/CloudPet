# No outputs in 3N-1: this configuration declares no resources or data
# sources. Outputs (account id, VPC id, subnet ids, security group ids, ALB
# DNS name, RDS endpoint, ECR repository URL, ...) are added incrementally as
# resources are introduced in later 3N milestones.
